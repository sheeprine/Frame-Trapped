<DockPanel x:Class="FrameTrapped.StreetFighterLibrary.ViewModels.ComboMakerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:cal="http://www.caliburnproject.org"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300" >
 
        <!-- Combo String -->
        <ItemsControl DockPanel.Dock="Top" ItemsSource="{Binding ComboString}">
            <ItemsPanelTemplate>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Height="30"/>
            </ItemsPanelTemplate>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock DockPanel.Dock="Left" Text="{Binding BaseMove.Name}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    <ContentControl cal:View.Model="{Binding ComboableMoves}" />
</DockPanel>